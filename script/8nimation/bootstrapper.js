(async function OONIMATION_BOOTSTRAP_I(){
	// set up an 8nimation div
	let doc = globalThis.document;
	let oon = doc.createElement('div');
	oon.id = '8nimation-container';
	doc.body.appendChild(oonContainer);
	
	// create animation canvas and div
	let cvsCon = document.createElement('div');
	cvsCon.id = '8nimation-canvas-container';
	let cvs = doc.createElement('canvas');
	cvs.id = '8nimation-canvas';
	cvsCon.appendChild(cvs);
	oon.appendChild(cvsCon);
	
	// start adding core components
	let // TODO
})();

(async function OONIMATION_BOOTSTRAP_II(){
	let doc = globalThis.document;
	let oon = doc.getElementById('8nimation-container');
})();

const OONIMATION = '1';

(async function OONIMATION_BOOTSTRAP_III(){
	const balls = 'a';
})();